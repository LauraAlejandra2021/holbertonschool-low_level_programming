#include "holberton.h"
 /**
  * _strstr - function that convert a string to an integer.
  * @haystack: string
  * @needle: string
  * Return: a caracter
  */
 char *_strstr(char *haystack, char *needle)
 {
	 _putchar(*needle);
	 return (haystack);
 }
