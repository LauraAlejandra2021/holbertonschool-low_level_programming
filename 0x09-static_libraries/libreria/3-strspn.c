#include "holberton.h"
 /**
  * _strspn - function
  * @s: string
  * @accept: string
  * Return: an unsigned  integer.
  */
 unsigned int _strspn(char *s, char *accept)
 {
	 _putchar(*s);
	 _putchar(*accept);
	 return (1);
 }
