#include "holberton.h"
 /**
  * _strpbrk - function
  * @s: string
  * @accept: string
  * Return: a pointer
  */
 char *_strpbrk(char *s, char *accept)
 {
	 _putchar(*s);
	 _putchar(*accept);
	 return (accept);
 }
