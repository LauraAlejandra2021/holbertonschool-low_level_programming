#include "holberton.h"
 /**
  * _atoi - function that convert a string to an integer.
  * @s: string
  * @c: caracter
  * Return: a pointer
  */
 char *_strchr(char *s, char c)
 {
	 _putchar(c);
	 return (s);
 }
